# Plan: Tiger Thumbnail + Logo Strip Marquee Redesign

## Task 1: Set Tiger Image as OG Thumbnail + Favicon

**Source file:** `public/thumbnail.png` (already exists)
**Target:** Social sharing preview (OG/Twitter) AND browser tab icon (favicon)

### Changes to `src/app/layout.tsx`:

1. **Favicon icons** (lines 52-59): Replace `/image.png` with `/thumbnail.png`
   ```ts
   icons: {
     icon: [{ url: '/thumbnail.png', type: 'image/png' }],
     apple: [{ url: '/thumbnail.png', sizes: '180x180', type: 'image/png' }],
   },
   ```

2. **Open Graph images** (lines 90-97): Replace `og-image.jpg` with `thumbnail.png`
   ```ts
   images: [{ url: "https://www.maacanimationjaipur.com/thumbnail.png", width: 1200, height: 630, alt: "..." }],
   ```

3. **Twitter card images** (line 104): Replace `og-image.jpg` with `thumbnail.png`

4. **JSON-LD image** (line 132): Replace `og-image.jpg` with `thumbnail.png`

### Also update `public/manifest.json`:
- Replace icon references from `/image.png` to `/thumbnail.png`

---

## Task 2: Full-Width Certification Logo Marquee

**Goal:** Replace the small 500px-centered scrollable logo strip with a full-width infinite marquee. Equal-sized logo cards, subtle continuous animation, responsive for mobile/laptop.

### Current state (HeroTrustTransition.tsx):
- Lines 13-19: certifications array with 5 entries (Skill India ×2, MESC ×2, NSDC)
- Lines 198-227: Scrollable strip with `max-w-[500px] mx-auto`, arrow buttons on mobile
- Part of a GSAP ScrollTrigger pinned morph animation

### New design:
- Full-width strip spanning the entire viewport
- Unique logos only: Skill India, MESC, NSDC (remove duplicates from array)
- Duplicate the set ×4 for seamless infinite loop
- Each logo in an **equal-sized card** with white background + `mix-blend-multiply`
- CSS `animate-marquee` (existing keyframe: `translateX(0)` → `translateX(-50%)`, 40s linear infinite)
- Gradient edge fades on left and right (same pattern as IndustryPartners.tsx)
- **No arrow navigation** — animation is continuous
- Hover pauses the animation (built into `.animate-marquee`)
- Respects `prefers-reduced-motion`

### Responsive:
- Desktop: logo cards ~`w-36 h-20`, gaps `gap-8`, padding `py-6`
- Mobile: logo cards ~`w-28 h-16`, gaps `gap-5`, padding `py-4`

### Implementation in HeroTrustTransition.tsx:
1. Deduplicate certifications array to 3 unique logos
2. Create `extendedLogos` = [...certs, ...certs, ...certs, ...certs] for seamless loop
3. Replace the `.morph-logos` section with a full-width marquee:
   - Remove `max-w-[500px] mx-auto` constraint
   - Remove arrow buttons and `scrollLogos` function
   - Add edge fade gradients
   - Use `flex whitespace-nowrap` + `animate-marquee`
4. Keep `.morph-logos` class on the wrapper for GSAP opacity/translateY animation

### Files to modify:
- `src/components/HeroTrustTransition.tsx` — certifications array + logos JSX section
- `src/app/layout.tsx` — metadata thumbnail references
- `public/manifest.json` — icon references

---

## Verification
1. `npm run dev` — check homepage loads
2. Hero morph animation works on scroll
3. Certification logos run as full-width infinite marquee below the morphed card
4. All 3 logos equally sized, seamless loop, edge fades present
5. Responsive: test at 375px, 768px, 1440px widths
6. OG preview: use opengraph.xyz or `curl` to verify tiger thumbnail shows
7. Browser tab shows tiger favicon
